Empezamos en una terminal cmd con
cd desktop 
npm create vite@latest
Nombre de la app
React
Javascript
cd 'nombre de la app' 
npm install
app .
npm run dev (reemplaza al npm start)

Y vamos


Crear componentes a partir de las vistas que vamos a usar
Ahora crear el ruteo
Instalo dependencias de react router dom : npm i react-router-dom 

A grandes rasgos haría un state, inicializado como obj vacío, que se llene con la data del JSON que venga desde el endpoint, si ese array está vacío, que muestre la página de inicio, o sea que en cada link, también tendría que hacer un seter de ese estadi como vacío. Esto para que se renderice una sola vista. Si quisiera que lo haga en otra, debería hacer un componente nuevo que aparezca cuando le doy click en buscar la ciudad 

En el footer renderizo primero que nada, en el return, el componente Outlet, previamente importado desde react router dom, que es el permiso para que los route que estén dentro de él en el app.jsx puedan ser renderizados.

En el app.jsx hago el routeo con el browserRouter, Routes y route pertinentes para que el footer englobe a todas las demás vistas 

instalo dependencia de axios para empezar a hacer las peticiones a la api. Pruebo en postman que el endpoint y la key funcionen correctamente



GUARDAR EN LOCAL STORAGE Y HACER UN BTN PARA LA BUSQUEDAA NUEVA